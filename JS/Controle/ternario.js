// (condição) ? 'valor1 para verdadeiro' : 'valor2 para falso'

const pontuacaoUsuario = 4000
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuario VIP' : 'Usuario normal'
console.log(nivelUsuario)